{
  "name": "opengallery-monorepo",
  "private": true,
  "scripts": {
    "dev": "./scripts/dev.sh",
    "publish": "./scripts/publish-unified.sh",
    "local": "./scripts/run-local.sh",
    "migrate": "npm run db:migrate --prefix api",
    "generate": "npm run db:generate --prefix api",
    "reset": "./scripts/reset.sh",
    "build": "npm run build --prefix packages/logger"
  },
  "workspaces": [
    "api",
    "web",
    "worker",
    "packages/*"
  ]
}
